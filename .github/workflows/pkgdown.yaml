name: site-deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  site:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          extra-repositories: https://tlverse.r-universe.dev

      - name: Check custom site sync
        shell: Rscript {0}
        run: source("tools/check_release_sync.R")

      - name: Validate committed site assets
        run: |
          test -f docs/index.html
          test -f docs/r.html
          test -f docs/sl3.html
          test -f docs/site.css
          test -f docs/site.js
          test -f docs/assets/hte3-badge-mark.svg

      - uses: peaceiris/actions-gh-pages@v4
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
